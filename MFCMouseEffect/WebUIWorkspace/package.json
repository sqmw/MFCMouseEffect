{
  "name": "mfcmouseeffect-webui-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "node scripts/clean-dist.mjs && pnpm run build:dialog && pnpm run build:shell && pnpm run build:workspace && pnpm run build:general && pnpm run build:mouse-companion && pnpm run build:effects && pnpm run build:text && pnpm run build:trail && pnpm run build:indicator && pnpm run build:automation && pnpm run build:wasm && node scripts/copy-output.mjs",
    "build:dialog": "vite build --mode dialog",
    "build:shell": "vite build --mode shell",
    "build:workspace": "vite build --mode workspace",
    "build:general": "vite build --mode general",
    "build:mouse-companion": "vite build --mode mouse-companion",
    "build:effects": "vite build --mode effects",
    "build:text": "vite build --mode text",
    "build:trail": "vite build --mode trail",
    "build:indicator": "vite build --mode indicator",
    "build:automation": "vite build --mode automation",
    "build:wasm": "vite build --mode wasm",
    "dev": "vite",
    "test:webui-dev-contract": "node scripts/test-webui-dev-contract.mjs",
    "test:webui-models": "pnpm run test:webui-dev-contract && pnpm run test:automation-platform && pnpm run test:automation-shortcuts && pnpm run test:automation-gesture-debug-model && pnpm run test:automation-gesture-id-preview && pnpm run test:general-state-model && pnpm run test:effects-profile-model && pnpm run test:input-indicator-plugin-menu-model && pnpm run test:wasm-error-model && pnpm run test:wasm-state-model && pnpm run test:wasm-diagnostics-model",
    "test:automation-platform": "node scripts/test-automation-platform.mjs",
    "test:automation-shortcuts": "node scripts/test-automation-shortcuts.mjs",
    "test:automation-gesture-debug-model": "node scripts/test-automation-gesture-debug-model.mjs",
    "test:automation-gesture-id-preview": "node scripts/test-automation-gesture-id-preview.mjs",
    "test:general-state-model": "node scripts/test-general-state-model.mjs",
    "test:effects-profile-model": "node scripts/test-effects-profile-model.mjs",
    "test:input-indicator-plugin-menu-model": "node scripts/test-input-indicator-plugin-menu-model.mjs",
    "test:wasm-error-model": "node scripts/test-wasm-action-error-model.mjs",
    "test:wasm-state-model": "node scripts/test-wasm-state-model.mjs",
    "test:wasm-diagnostics-model": "node scripts/test-wasm-diagnostics-model.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.0"
  }
}
